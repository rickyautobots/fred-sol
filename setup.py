#!/usr/bin/env python3
"""
FRED-SOL Setup Script

For editable installation:
    pip install -e .

For development:
    pip install -e ".[dev]"

For full installation with dashboard:
    pip install -e ".[full]"
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
